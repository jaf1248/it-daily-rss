<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
  <title>Joe SysAdmin and Operations</title>
  <description>Operational IT changes, Microsoft updates, outages, and infrastructure issues.</description>
  <lastBuildDate>Tue, 26 May 2026 21:42:07 +0000</lastBuildDate>
  
<item>
  <title>🔵 Microsoft Defender can now automatically isolate hacked endpoints</title>
  <link>https://www.bleepingcomputer.com/news/microsoft/microsoft-defender-can-now-automatically-isolate-hacked-endpoints/</link>
  <pubDate>Tue, 26 May 2026 12:19:43 +0000</pubDate>
  <source>BleepingComputer</source>
  <description><![CDATA[<b>Why this matters: Endpoint detection changes — review high-severity detections and alert noise.</b><br/>Microsoft is testing a new Defender for Endpoint capability that will automatically isolate compromised endpoints to thwart attackers' attempts to move laterally across the network. [...]<br/><br/><b>Source:</b> BleepingComputer &nbsp;|&nbsp; <b>Severity:</b> FYI &nbsp;|&nbsp; <b>Age:</b> 9h ago]]></description>
</item>

<item>
  <title>🔵 Entra Automation: Users, Groups, and Conditional Access</title>
  <link>https://adamtheautomator.com/entra-automation-users-groups-conditional-access/</link>
  <pubDate>Mon, 25 May 2026 18:00:00 +0000</pubDate>
  <source>Adam the Automator</source>
  <description><![CDATA[<b>Why this matters: Could impact sign-ins/MFA/Conditional Access — watch for login issues.</b><br/>Use Microsoft Graph PowerShell to automate Entra ID users, security groups, group membership, and Conditional Access policies with report-only validation.<br/><br/><b>Source:</b> Adam the Automator &nbsp;|&nbsp; <b>Severity:</b> FYI &nbsp;|&nbsp; <b>Age:</b> 1d ago]]></description>
</item>

<item>
  <title>🔵 FBI warns of Kali365 phishing service targeting Microsoft 365 accounts</title>
  <link>https://www.bleepingcomputer.com/news/security/fbi-warns-of-kali365-phishing-service-targeting-microsoft-365-accounts/</link>
  <pubDate>Mon, 25 May 2026 12:45:54 +0000</pubDate>
  <source>BleepingComputer</source>
  <description><![CDATA[<b>Why this matters: Credential/phishing risk — reinforce MFA and user awareness training.</b><br/>The FBI is warning about the Kali365 phishing-as-a-service platform (PhaaS) that is used to hijack Microsoft 365 accounts by abusing OAuth device code authentication to steal session tokens and bypass multi-factor authentication (MFA). [...]<br/><br/><b>Source:</b> BleepingComputer &nbsp;|&nbsp; <b>Severity:</b> FYI &nbsp;|&nbsp; <b>Age:</b> 1d ago]]></description>
</item>

<item>
  <title>🔵 How to use Instance Mix with Azure Virtual Machine Scale Sets</title>
  <link>https://techcommunity.microsoft.com/t5/itops-talk-blog/how-to-use-instance-mix-with-azure-virtual-machine-scale-sets/ba-p/4522574</link>
  <pubDate>Sun, 24 May 2026 23:38:37 +0000</pubDate>
  <source>Microsoft Tech Community IT Ops</source>
  <description><![CDATA[<b>Why this matters: RCE vulnerability — patch immediately, check for exposed attack surface.</b><br/><p>When a Virtual Machine Scale Set needs to scale out, it normally uses a single VM size (also known as SKU). Whilst that is simple, it can also become a constraint. For example the scale out might be limited if that specific size has limited regional capacity, quota pressure, or a price profile that is not ideal for every scale-out event.</p>
<p>Instance Mix for Azure Virtual Machine Scale Sets allows you to configure your scale out with more options by define multiple compatible VM sizes in a single scale set. When scaling out, Azure can choose from your list during provisioning based on the allocation strategy you select.</p>
<h2>What is Instance Mix?</h2>
<p>Instance Mix is a Virtual Machine Scale Sets capability that lets you specify up to five VM sizes for a single scale set that uses Flexible orchestration mode. Instead of setting the scale set to a single size such as <code>Standard_D2s_v5</code>, you set the scale set SKU name to <code>Mix</code> and define the real VM sizes in the <code>skuProfile</code>.</p>
<p>At provisioning time, Azure uses the <code>skuProfile.vmSizes</code> list and the selected allocation strategy to decide which VM size to deploy.</p>
<p>The high-level model looks like this:</p>
<pre class="language-json" tabindex="0"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"sku"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Mix"</span><span class="token punctuation">,</span>
    <span class="token property">"capacity"</span><span class="token operator">:</span> <span class="token number">2</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token property">"properties"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token property">"skuProfile"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token property">"vmSizes"</span><span class="token operator">:</span> <span class="token punctuation">[</span>
        <span class="token punctuation">{</span> <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2s_v5"</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token punctuation">{</span> <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2as_v5"</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token punctuation">{</span> <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2s_v4"</span> <span class="token punctuation">}</span>
      <span class="token punctuation">]</span><span class="token punctuation">,</span>
      <span class="token property">"allocationStrategy"</span><span class="token operator">:</span> <span class="token string">"CapacityOptimized"</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre>
<p>You can use this if your workload can run on more than one VM size and you want to improve provisioning success, optimize for cost, or align allocation with reservations or savings plans.</p>
<h2>When should you use Instance Mix?</h2>
<p>Instance Mix is a good fit when:</p>
<ul>
<li>Your application can run correctly on several similar VM sizes.</li>
<li>You want scale-out to have more than one capacity option in a region.</li>
<li>You run Spot VMs and want Azure to prefer lower-priced capacity when available.</li>
<li>You have reservations for specific sizes, or want to favor VM sizes with better savings-plan economics, and need predictable priority.</li>
<li>You want one scale set instead of several separate scale sets for equivalent worker capacity.</li>
</ul>
<p>It is usually best for stateless or horizontally scalable workloads, such as web front ends, API tiers, queue workers, batch workers, and other scale-out services where each instance performs the same role.</p>
<p>Avoid using Instance Mix as a way to combine very different machines in one pool. For example, mixing a small general-purpose VM with a much larger memory-optimized VM can make capacity planning, load distribution, and performance troubleshooting harder. Best practice is to use sizes with similar vCPU and memory for balanced load distribution and similar VM types for consistent performance.</p>
<h2>Key requirements and limits</h2>
<p>Before you deploy, check these requirements:</p>
<div class="styles_lia-table-wrapper__h6Xo9 styles_table-responsive__MW0lN"><table><thead><tr><th>Requirement</th><th>Detail</th></tr></thead><tbody><tr><td>Orchestration mode</td><td>Instance Mix is available only for Virtual Machine Scale Sets using Flexible orchestration mode.</td></tr><tr><td>Number of VM sizes</td><td>You can specify up to five VM sizes.</td></tr><tr><td>VM family support</td><td>The Instance Mix <code>skuProfile</code> supports A, B, D, E, and F VM families.</td></tr><tr><td>Architecture</td><td>Do not mix CPU architectures. For example, do not mix Arm64 and x64 sizes in the same Instance Mix.</td></tr><tr><td>Storage interface</td><td>Do not mix incompatible storage interfaces such as SCSI and NVMe.</td></tr><tr><td>Premium storage capability</td><td>Do not mix VM SKUs that use premium storage with SKUs that do not.</td></tr><tr><td>Security profile</td><td>The selected sizes must use a compatible security profile.</td></tr><tr><td>Local disk configuration</td><td>The selected sizes must have a consistent local disk configuration.</td></tr><tr><td>Quota</td><td>You must already have quota for the VM sizes you include. Instance Mix does not request quota for you.</td></tr><tr><td>Unsupported scenarios</td><td>Instance Mix currently does not support Standby Pools, Azure Dedicated Host, Proximity Placement Groups, on-demand capacity reservations, or <code>diffDiskSettings</code> on the OS disk.</td></tr></tbody></table></div>
<p>Microsoft Learn currently states that all public Azure regions support Instance Mix. VM size availability and quota still vary by region and subscription, so you should check the exact sizes you plan to use before deploying.</p>
<h2>Choose an allocation strategy</h2>
<p>Instance Mix supports three allocation strategies.</p>
<div class="styles_lia-table-wrapper__h6Xo9 styles_table-responsive__MW0lN"><table><thead><tr><th>Strategy</th><th>Best for</th><th>Behavior</th></tr></thead><tbody><tr><td><code>LowestPrice</code></td><td>Cost-sensitive and fault-tolerant workloads, especially Spot</td><td>Azure prefers the lowest-priced VM sizes in the list while considering available capacity. It deploys as many of the lowest-priced VMs as capacity allows before moving to higher-priced sizes, so higher-cost sizes may be selected to secure capacity. This is the default strategy if you do not specify one.</td></tr><tr><td><code>CapacityOptimized</code></td><td>Production workloads where provisioning success is more important than the lowest possible price</td><td>Azure prioritizes VM sizes that have the highest likelihood of available capacity in the target region. Cost is not considered, and higher-cost sizes may be selected to secure capacity.</td></tr><tr><td><code>Prioritized</code></td><td>Predictable ordering, reservations, or savings plan alignment</td><td>Azure follows user-defined <code>rank</code> values on VM sizes when provided, subject to quota and regional capacity. Lower rank values have higher priority. This strategy is currently documented as preview.</td></tr></tbody></table></div>
<p>Rank note: Use <code>rank</code> only with <code>Prioritized</code>. Ranks are optional with <code>Prioritized</code>; when specified, lower numbers have higher priority, and rank values can be duplicated or non-sequential. Omit ranks for <code>LowestPrice</code> and <code>CapacityOptimized</code>.</p>
<p>For many operations teams,&nbsp;<code>CapacityOptimized</code> is a practical starting point for production scale sets because it is designed to improve the chance of successful provisioning. For Spot-heavy or highly cost-sensitive workloads, <code>LowestPrice</code> may be a better first choice. If you have reservations for specific sizes, or want to favor VM sizes with better savings-plan economics, use <code>Prioritized</code> and rank those reservation-backed or preferred sizes first.</p>
<h2>Walkthru Prereqs</h2>
<p>The commands below are Bash-specific. Use Azure Cloud Shell in Bash mode or another Bash environment with Azure CLI installed.</p>
<p>You need:</p>
<ul>
<li>An Azure subscription.</li>
<li>Permission to create resource groups, networking, and Virtual Machine Scale Sets.</li>
<li>Azure CLI version 2.66.0 or later.</li>
<li>An SSH-capable client if you plan to connect to the Linux instances.</li>
</ul>
<p>Check your Azure CLI version:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az version --query '"azure-cli"' --output tsv
</code></pre>
<p>Sign in and select the subscription you want to use:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az login
az account set --subscription "&lt;subscription-id-or-name&gt;"
</code></pre>
<p>Set variables for the example:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">RG="rg-vmss-instancemix-demo"
LOCATION="australiaeast"
VMSS="vmss-instancemix-demo"
ADMIN_USER="azureuser"
</code></pre>
<p>You can change <code>LOCATION</code> to another public Azure region if you don't feel like sharing a datacenter rack with drop bears and the occasional kangaroo. Keep the VM sizes in the examples compatible if you change regions or families.</p>
<h2>Step 1: Check VM size availability in the region</h2>
<p>Before creating the scale set, check that the candidate VM sizes are available in your target region and subscription.</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vm list-skus \
  --location "$LOCATION" \
  --resource-type virtualMachines \
  --size Standard_D \
  --all \
  --output table
</code></pre>
<p>For this walkthrough, we will use these D-series sizes:</p>
<ul>
<li><code>Standard_D2s_v5</code></li>
<li><code>Standard_D2as_v5</code></li>
<li><code>Standard_D2s_v4</code></li>
</ul>
<p>In the <code>az vm list-skus</code> output, check the <code>Restrictions</code> column. If a size is restricted in your region or subscription, choose another compatible size or deploy in a different region.</p>
<h2>Step 2: Check quota</h2>
<p>Instance Mix does not automatically request quota. If one size in the mix lacks quota, Azure can try another size from the list that has quota. If none of the eligible sizes has enough quota, deployment or scale-out can fail.</p>
<p>Check current regional VM quota and usage:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vm list-usage \
  --location "$LOCATION" \
  --output table
</code></pre>
<p>Look for:</p>
<ul>
<li><code>Total Regional vCPUs</code></li>
<li>The family quota rows that match your selected VM sizes, such as DSv5, DASv5, or DSv4 family vCPUs</li>
</ul>
<p>If quota is too low, either request a quota increase or choose sizes that have available quota in the target region.</p>
<h2>Step 3: Create a resource group</h2>
<p>Create a resource group for the demo:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az group create \
  --name "$RG" \
  --location "$LOCATION"
</code></pre>
<h2>Step 4: Create a VM Scale Set with Instance Mix</h2>
<p>The key settings are:</p>
<ul>
<li><code>--orchestration-mode Flexible</code></li>
<li><code>--vm-sku Mix</code></li>
<li><code>--skuprofile-vmsizes</code></li>
<li><code>--skuprofile-allocation-strategy</code></li>
</ul>
<p>Create the scale set:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss create \
  --resource-group "$RG" \
  --name "$VMSS" \
  --location "$LOCATION" \
  --orchestration-mode Flexible \
  --image Ubuntu2204 \
  --vm-sku Mix \
  --skuprofile-vmsizes Standard_D2s_v5 Standard_D2as_v5 Standard_D2s_v4 \
  --skuprofile-allocation-strategy CapacityOptimized \
  --instance-count 2 \
  --admin-username "$ADMIN_USER" \
  --generate-ssh-keys
</code></pre>
<p>This creates a Flexible orchestration scale set with two instances. Because the scale set uses Instance Mix, the scale set SKU name is <code>Mix</code>, and the allowed VM sizes are stored in <code>skuProfile.vmSizes</code>.</p>
<p>Do not expect every VM size in the list to appear immediately. Azure may deploy all initial instances using the same VM size if that satisfies the selected allocation strategy and capacity is available. The point of Instance Mix is to give Azure more eligible choices during provisioning and scaling.</p>
<h2>Step 5: Verify the Instance Mix configuration</h2>
<p>View the complete <code>skuProfile</code>:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query "skuProfile" \
  --output jsonc
</code></pre>
<p>Example output:</p>
<pre class="language-json" tabindex="0"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"allocationStrategy"</span><span class="token operator">:</span> <span class="token string">"CapacityOptimized"</span><span class="token punctuation">,</span>
  <span class="token property">"vmSizes"</span><span class="token operator">:</span> <span class="token punctuation">[</span>
    <span class="token punctuation">{</span>
      <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2s_v5"</span>
    <span class="token punctuation">}</span><span class="token punctuation">,</span>
    <span class="token punctuation">{</span>
      <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2as_v5"</span>
    <span class="token punctuation">}</span><span class="token punctuation">,</span>
    <span class="token punctuation">{</span>
      <span class="token property">"name"</span><span class="token operator">:</span> <span class="token string">"Standard_D2s_v4"</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">]</span>
<span class="token punctuation">}</span>
</code></pre>
<p>View only the configured VM sizes:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query "skuProfile.vmSizes[].name" \
  --output tsv
</code></pre>
<p>View only the allocation strategy:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query "skuProfile.allocationStrategy" \
  --output tsv
</code></pre>
<p>List the current instances and their VM sizes. Because Flexible orchestration instances are standard Azure VMs, use <code>az vm list</code> for full instance details and filter by the scale set resource ID:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">VMSS_ID=$(az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query id \
  --output tsv)

az vm list \
  --resource-group "$RG" \
  --query "[?virtualMachineScaleSet.id=='$VMSS_ID'].{Name:name, VMSize:hardwareProfile.vmSize, ProvisioningState:provisioningState}" \
  --output table
</code></pre>
<h2>Step 6: Scale out and observe allocation</h2>
<p>Scale the set from two instances to five:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss scale \
  --resource-group "$RG" \
  --name "$VMSS" \
  --new-capacity 5
</code></pre>
<p>List the instances again:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">VMSS_ID=$(az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query id \
  --output tsv)

az vm list \
  --resource-group "$RG" \
  --query "[?virtualMachineScaleSet.id=='$VMSS_ID'].{Name:name, VMSize:hardwareProfile.vmSize, ProvisioningState:provisioningState}" \
  --output table
</code></pre>
<p>You may see one VM size or several VM sizes. The result depends on allocation strategy, quota, regional capacity, and the sizes in your <code>skuProfile</code>.</p>
<h2>Step 7: Add autoscale</h2>
<p>Instance Mix does not require autoscale, but it becomes especially useful when a workload scales out under demand. Autoscale asks the scale set to add instances; Instance Mix gives Azure multiple eligible VM sizes to use for those new instances.</p>
<p>Create an autoscale profile:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az monitor autoscale create \
  --resource-group "$RG" \
  --resource "$VMSS" \
  --resource-type Microsoft.Compute/virtualMachineScaleSets \
  --name "vmss-autoscale" \
  --min-count 2 \
  --max-count 10 \
  --count 2
</code></pre>
<p>Create a scale-out rule that adds two instances when average CPU is greater than 70 percent for five minutes:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az monitor autoscale rule create \
  --resource-group "$RG" \
  --autoscale-name "vmss-autoscale" \
  --condition "Percentage CPU &gt; 70 avg 5m" \
  --scale out 2
</code></pre>
<p>Create a scale-in rule that removes one instance when average CPU is less than 30 percent for five minutes:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az monitor autoscale rule create \
  --resource-group "$RG" \
  --autoscale-name "vmss-autoscale" \
  --condition "Percentage CPU &lt; 30 avg 5m" \
  --scale in 1
</code></pre>
<p>Tune these thresholds for your workload. Scale out aggressively enough to protect availability, and scale in conservatively enough to avoid unnecessary churn.</p>
<h2>Step 8: Update the VM sizes in the mix</h2>
<p>You can change the VM sizes in an existing Instance Mix configuration. The important detail is that updating <code>--skuprofile-vmsizes</code> replaces the full list. It does not add or remove a single size incrementally.</p>
<p>For example, this command replaces the mix with four sizes:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss update \
  --resource-group "$RG" \
  --name "$VMSS" \
  --skuprofile-vmsizes Standard_D2s_v5 Standard_D2as_v5 Standard_D2s_v4 Standard_D2as_v4
</code></pre>
<p>If you want to remove a size, specify the full list of sizes you want to keep. If you want to add a size, specify the full existing list plus the new size.</p>
<p>Verify the new list:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss show \
  --resource-group "$RG" \
  --name "$VMSS" \
  --query "skuProfile.vmSizes[].name" \
  --output tsv
</code></pre>
<h2>Step 9: Change the allocation strategy</h2>
<p>You can also update the allocation strategy.</p>
<p>For example, this changes the walkthrough scale set from its original <code>CapacityOptimized</code> strategy to <code>LowestPrice</code>. Use this as a real update example, not as a blanket production recommendation:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss update \
  --resource-group "$RG" \
  --name "$VMSS" \
  --set skuProfile.allocationStrategy=LowestPrice
</code></pre>
<p>When you change the allocation strategy, existing VMs are not immediately reshaped. The new strategy takes effect after the scale set scales in or out.</p>
<p>If you change from <code>Prioritized</code> to another allocation strategy, Microsoft Learn notes that you must first nullify the priority ranks associated with the VM sizes.</p>
<h2>Step 10: Enable Instance Mix on an existing scale set</h2>
<p>You can enable Instance Mix on a separate existing scale set if it uses Flexible orchestration mode, does not already use Instance Mix, and the selected VM sizes are compatible. Do not run this procedure against the <code>$VMSS</code> scale set created earlier, because that scale set is already Instance Mix-enabled.</p>
<p>The required settings are:</p>
<ul>
<li>Set <code>sku.name</code> to <code>Mix</code>.</li>
<li>Set <code>sku.tier</code> explicitly to <code>null</code> unless you have confirmed it is already <code>null</code> or absent.</li>
<li>Provide at least one VM size in <code>skuProfile.vmSizes</code>.</li>
<li>Provide an allocation strategy, or let Azure use the default lowest-price strategy.</li>
</ul>
<p>Set a separate variable for the existing scale set you want to convert:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">EXISTING_VMSS="my-existing-flex-vmss"
</code></pre>
<p>Example:</p>
<pre class="language-azurecli" tabindex="0"><code class="language-azurecli">az vmss update \
  --resource-group "$RG" \
  --name "$EXISTING_VMSS" \
  --set sku.name=Mix sku.tier=null \
  --skuprofile-vmsizes Standard_D2as_v4 Standard_D2s_v5 Standard_D2as_v5 \
  --set skuProfile.allocationStrategy=CapacityOptimized
</code></pre>
<p>As with other Instance Mix updates, existing VM instances are not necessarily changed immediately. The configuration is used for subsequent scale actions.</p>
<h2>Portal procedure</h2>
<p>If you prefer the Azure portal:</p>
<ol>
<li>Go to <strong>Virtual machine scale sets</strong>.</li>
<li>Select <strong>Create</strong>.</li>
<li>On the <strong>Basics</strong> tab, choose the subscription, resource group, scale set name, region, image, and administrator settings.</li>
<li>Set <strong>Orchestration mode</strong> to <strong>Flexible</strong>.</li>
<li>In the <strong>Size</strong> section, choose <strong>Select up to 5 sizes</strong>.</li>
<li>Select up to five compatible VM sizes.</li>
<li>Choose the <strong>Allocation strategy</strong>.</li>
<li>If you choose <strong>Prioritized (preview)</strong>, a <strong>Rank size</strong> section appears below <strong>Allocation strategy</strong>. Select <strong>Rank priority</strong> to open the prioritization blade and order the VM sizes based on your preferred priority.</li>
<li>Complete the remaining tabs for networking, management, health, scaling, and advanced settings.</li>
<li>Select <strong>Review + create</strong>, then create the scale set.</li>
</ol>
<p>After deployment, open the scale set and check the <strong>Overview</strong> blade. In the <strong>Properties</strong> section, check <strong>Size</strong> for the configured VM sizes and <strong>Management</strong> for the allocation strategy.</p>
<h2>Operational tips</h2>
<h3>Use similar sizes for predictable application behavior</h3>
<p>For web, API, and worker tiers, choose sizes with similar vCPU and memory. This makes autoscale behavior easier to reason about and helps load distribution remain balanced.</p>
<h3>Do not rely on Instance Mix as a quota workaround</h3>
<p>Instance Mix can use another listed size if one size lacks quota, but it does not request quota for you. Check quota before production deployment, especially if your autoscale maximum is high.</p>
<h3>Align reservations and savings plans with <code>Prioritized</code></h3>
<p>Reserved instance pricing and savings-plan discounts can apply with Instance Mix. If you want to consume reservation-backed sizes first, or favor sizes with better savings-plan economics, use the <code>Prioritized</code> strategy and rank those sizes first.</p>
<h3>Use Spot Priority Mix for Spot plus Standard</h3>
<p>Instance Mix can be used with both Spot and Standard VMs. The <a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/instance-mix-faq-troubleshooting" rel="noopener noreferrer" target="_blank">Instance Mix FAQ</a> says to use <a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/spot-priority-mix" rel="noopener noreferrer" target="_blank">Spot Priority Mix</a> when you need a defined split between Spot and Standard capacity.</p>
<p>Caveat: Instance Mix supports B-family sizes for regular capacity, but Azure Spot VMs do not support B-series or promo versions of any size. If your scale set will use Spot capacity, choose Spot-supported sizes for the mix.</p>
<h3>Be careful with unsupported properties</h3>
<p>If a deployment template includes unsupported properties such as Azure Dedicated Host, on-demand capacity reservation, Standby Pools, Proximity Placement Groups, or OS disk <code>diffDiskSettings</code>, remove those settings before using Instance Mix.</p>
<h2>Troubleshooting common deployment errors</h2>
<div class="styles_lia-table-wrapper__h6Xo9 styles_table-responsive__MW0lN"><table><thead><tr><th>Error code</th><th>Meaning</th><th>Fix</th></tr></thead><tbody><tr><td><code>SkuProfileAllocationStrategyInvalid</code></td><td>The allocation strategy is not valid.</td><td>Use <code>CapacityOptimized</code>, <code>Prioritized</code>, or <code>LowestPrice</code> for the allocation strategy.</td></tr><tr><td><code>SkuProfileVMSizesCannotBeNullOrEmpty</code></td><td>No VM sizes were provided.</td><td>Add at least one VM size to <code>skuProfile.vmSizes</code>.</td></tr><tr><td><code>SkuProfileHasTooManyVMSizesInRequest</code></td><td>More than five VM sizes were specified.</td><td>Reduce the list to no more than five VM sizes.</td></tr><tr><td><code>SkuProfileVMSizesCannotHaveDuplicates</code></td><td>The same VM size appears more than once.</td><td>Remove duplicate VM sizes.</td></tr><tr><td><code>SkuNameMustBeMixIfSkuProfileIsSpecified</code></td><td>A <code>skuProfile</code> was provided, but <code>sku.name</code> is not <code>Mix</code>.</td><td>Set <code>sku.name</code> to <code>Mix</code>.</td></tr><tr><td><code>SkuTierMustNotBeSetIfSkuProfileIsSpecified</code></td><td><code>sku.tier</code> is set when using Instance Mix.</td><td>Set <code>sku.tier</code> to <code>null</code> or omit it.</td></tr><tr><td><code>SkuProfileScenarioNotSupported</code></td><td>The template includes a property not supported with Instance Mix.</td><td>Remove unsupported properties such as host groups, capacity reservations, or standby pool settings.</td></tr></tbody></table></div>
<p>&nbsp;</p>
<p>Instance Mix gives your operations team a practical way to make scale sets more flexible. By defining several compatible VM sizes and selecting the right allocation strategy, you can improve scale-out success, give Azure more capacity choices, and better align VM allocation with cost or reservation goals.</p>
<p>For most production workloads, start with similar VM sizes, verify regional availability and quota, use Flexible orchestration mode, and test scale-out behavior before relying on the configuration in a critical environment.</p>
<h2>Further reading on Microsoft Learn:</h2>
<p><a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/instance-mix-overview" rel="noopener noreferrer" target="_blank">Use multiple Virtual Machine sizes with instance mix</a></p>
<p><a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/instance-mix-create" rel="noopener noreferrer" target="_blank">Create a scale set using instance mix</a></p>
<p><a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-orchestration-modes" rel="noopener noreferrer" target="_blank">Orchestration modes for Virtual Machine Scale Sets</a></p>
<p><a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/instance-mix-update" rel="noopener noreferrer" target="_blank">Update instance mix settings on an existing scale set</a></p>
<p><a href="https://learn.microsoft.com/azure/virtual-machine-scale-sets/instance-mix-faq-troubleshooting" rel="noopener noreferrer" target="_blank">Instance Mix FAQ and troubleshooting</a></p><br/><br/><b>Source:</b> Microsoft Tech Community IT Ops &nbsp;|&nbsp; <b>Severity:</b> FYI &nbsp;|&nbsp; <b>Age:</b> 1d ago]]></description>
</item>

</channel>
</rss>
